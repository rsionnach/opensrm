# Minimal Service Reliability Manifest
# This example shows the minimum required fields

apiVersion: srm/v1
kind: ServiceReliabilityManifest
metadata:
  name: my-service
  team: my-team
  tier: standard

spec:
  slos:
    availability:
      target: 0.999
      window: 30d
