# INVALID: apiVersion 'opensrm/v1' != required const opensrm.nthlayer.io/v2
apiVersion: opensrm/v1
kind: JudgmentSLO
metadata: { name: x }
spec:
  service: x
  judgment_type: reversal_rate
  measurement: { window: 7d, source: lineage }
  target: { maximum_reversal_rate: 0.05 }
