# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor directories and files
.idea/
.vscode/
*.code-workspace
*.swp
*.swo
*~

# Node modules (for validation tooling)
node_modules/
action/node_modules/

# Test output
coverage/
.nyc_output/

# Build output
dist/
!action/dist/
build/

# Local environment
.env
.env.local

# Logs
*.log
npm-debug.log*

# Temporary files
tmp/
temp/

# Dolt database files (added by bd init)
.dolt/
*.db

# Local backups created by the auto-memory retirement plan (2026-06-04)
*.bak

# Worktrees Claude Code creates for --worktree and for subagents running with
# `isolation: worktree`. Safe to use in THIS repo (spec only — no Python, no
# sibling path dependencies); the other ecosystem members must use sibling
# worktrees instead, via .claude/bin/eco-worktree.sh at the ecosystem root.
.claude/worktrees/
